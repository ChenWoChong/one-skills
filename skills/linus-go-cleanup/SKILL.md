---
name: linus-go-cleanup
description: 用 Linus 风格做 PR 提交前最后一轮严格守门和 Go 代码清理。适用于所有 Go 代码的 PR 合并前最终优化、降低复杂度、提升可读性、删除冗余代码、删除无意义 nil 或空值防御判断、删除一行包装函数和无实际使用的接口、避免为抽函数增加代码量、减少 if/else 嵌套、检查职责边界、补充有排查价值的日志，以及确保测试贴近真实生产流程而不是逼生产代码增加防御性分支。发现问题时要直说并阻止草率合并；用户同意修改后立即落地代码并验证。
---

# Linus Go Cleanup

## 目标

在提交 PR 前，对 Go 代码做一次强硬、务实的清理。优先保证正确性、主流程清晰、抽象最少、测试贴近生产真实路径。不要因为“看起来更安全”就加代码；只有真实数据流可能产生的状态，才值得写判断。

最终代码必须比修改前更容易读、更少路径、更少间接层、更少状态。新增的每一行、每一个分支、每一个类型、每一个接口都必须证明自己降低了整体复杂度。

## 守门员模式

把这个 skill 当成 PR 提交前最后一道门。目标不是给温和建议，而是把会降低代码质量、制造长期负担、掩盖真实问题的改动挡下来。

审查时要绝对严格：

- 发现问题就直接指出，不要为了语气好听而弱化判断。
- 先区分必须改、建议改、可以不动；必须改的问题不能用“看起来还行”放过。
- 如果用户只是要求审查，先列出问题和建议改法，不擅自改代码。
- 如果用户要求清理、修复、优化，或者已经同意某个改法，就立即修改代码，不停在建议层。
- 如果改动会改变对外 API、持久化语义、并发行为、错误语义或测试策略，先把风险说清楚并请求用户确认；用户确认后立刻实施并验证。
- 不接受“以后可能有用”“这样更工程化”“方便测试”这类没有当前生产路径支撑的理由。
- 收尾必须说明改了什么、验证了什么、还剩什么风险。

## 工作流程

1. 先建立上下文：看调用路径、构造函数、测试、真实运行时 wiring。
2. 先理清数据生命周期：谁创建、谁持有、谁修改、谁持久化、谁关闭。
3. 按严重程度列出问题：必须改、建议改、可以不动。
4. 删除假的特殊情况：删掉只为测试方便或想象中未来需求存在的分支、接口、包装函数和字段。
5. 保持兼容：除非用户明确接受，否则不要改变对外 API 行为。
6. 用户同意后立即修改代码，并把修改控制在这次 PR 的真实范围内。
7. 同步文档：每次改动都主动判断 README、docs、配置示例、测试说明和代码注释是否需要更新。
8. 最后验证：先跑改动包的定向测试，再跑项目要求的最终检查。

## 规则

### 先做减法

默认从删除开始，而不是从新增开始。优先删除冗余分支、重复状态、薄包装函数、假接口、无效配置、无意义测试 fake 和不会发生的防御判断。

任何新增代码都必须满足至少一个条件：

- 删除了更多代码或更多分支。
- 让主流程明显更直。
- 把职责移回真正拥有数据的一层。
- 消除了真实 bug 或真实并发、资源、持久化风险。

如果新增代码只是让结构“看起来更完整”，但没有减少复杂度，就不要加。

### 控制复杂度预算

每次修改都要检查净复杂度是否下降：

- 分支数量不能无理由增加。
- struct 字段、接口、配置项、全局变量不能无理由增加。
- goroutine、channel、锁、缓存、状态机不能为了想象中的需求新增。
- 测试 fake、mock、helper 不能比被测逻辑更复杂。
- 为了复用而复用、为了模式而模式，都要删掉。

如果修改后代码行数增加，但调用路径、状态数量、理解成本没有下降，这个修改应当被拒绝。

### 删除无意义防御代码

如果生产流程不可能产生某个 `nil`、空字符串、空 slice 或零值，就不要新增或保留对应判断。

优先修调用方、构造函数或测试 fake，不要为了测试场景给生产代码加防御分支。

这些判断可以保留，因为它们代表真实业务状态或外部输入：

```go
if southConn == nil { ... }      // 真实连接状态
if southID == "" { ... }         // 真实未绑定 session 状态
if err != nil { ... }            // Go 错误处理
```

如果构造流程已经保证依赖存在，就删掉这类判断：

```go
if m.store == nil { return nil } // 坏：隐藏了非法 Manager
if h.lifecycle == nil { ... }    // 坏：除非 lifecycle 真的是可选依赖
```

### 删除薄包装函数

如果一个函数只有一次真实调用，再加一点参数转发或名字包装，并且内联后更易读，就删除它。

这条要严格执行：

- 只有一个调用点的一行 helper，默认删掉，除非它表达了稳定领域概念。
- 如果它只是把 `a.b(c)` 包成 `x(c)`，没有新增策略、没有隐藏复杂实现、没有统一副作用，就不要保留。
- 不要为了让函数“看起来更短”就把简单判断、简单赋值、简单转发拆进 helper。读者为了看 3 行代码来回跳一次，通常只会更难读。
- 一组名字泛泛的 helper，比如 `ensureX`、`handleY`、`processZ`、`closeIfNeeded`，往往说明职责没理顺，只是在挪代码，不是在降复杂度。

坏例子：

```go
func (h *NorthHandler) touchSession(id acp.SessionID) {
	h.lifecycle.MarkActive(id)
}
```

更好：

```go
h.lifecycle.MarkActive(id)
```

只有在包装函数表达稳定领域概念、集中非平凡策略、隐藏易变实现细节，或明显提升调用点可读性时，才保留。

### 谨慎新增函数

新增函数必须减少真实复杂度、表达稳定领域概念，或隔离一段非平凡策略。

如果只是把几行代码搬到另一个函数里，导致调用点、参数传递和函数定义加起来的总代码量增加，并且可读性没有明显提升，就不要新增这个函数。

尤其警惕这种伪优化：

- 原函数本来十几行直着就能读完，拆成 3 到 5 个小 helper 后，读者反而要上下跳转。
- helper 之间共享同一批局部变量和状态，参数传来传去，暴露出真正该收敛的是数据结构或职责边界。
- 为了复用两段相似代码硬抽函数，但被抽出来的只有调用顺序一样，没有稳定语义，最后把差异塞进 bool 参数或回调里。

拆函数前先问：

- 内联后是否更短、更直接？
- 新函数名是否表达了调用点看不出来的领域语义？
- 参数列表是否暴露出职责放错了位置？
- 这个函数是否只有一个调用点，且没有形成稳定抽象？

函数名不能替代码解释废话。`handleFoo`、`processBar`、`doThing` 这类名字如果没有表达额外领域语义，就不是抽函数的理由。

### 避免假接口

不要为了让一个 struct 调一个方法就新建接口，尤其是生产代码里没有第二个实现时。

依赖归属清楚时，优先用具体类型：

```go
lifecycle *session.Lifecycle
```

接口只放在真实边界：外部系统、多生产实现的测试缝、或者包之间确实不能依赖具体类型的地方。

不要为了测试方便把具体依赖抽成接口。优先让测试走真实构造流程，或者在真实边界处替换外部系统。

### 保持主流程扁平

让 happy path 保持在函数最外层。可以用 early return 减少嵌套，但不要为了形式上的“扁平”增加代码量。

重写 `if/else` 时，优先缩小 `if` 内部的代码块，让异常路径提前返回，主流程留在函数体最外层。只有在重写后代码量没有增加，或者可读性明显变好时，才做这类调整。

这种错误处理可以保留，不要强行压平：

```go
if err != nil {
	if errors.Is(err, context.Canceled) {
		return nil
	}
	return err
}
```

避免把大半个函数包在 `if ok { ... } else { ... }` 里。能用 early return 消掉失败路径，就让主流程露出来。

不要把简单条件拆成多层 helper 来制造“扁平”。如果读者必须跳来跳去才能理解主流程，这不是简化。

业务状态分支和错误处理不要伪装成同一层条件。`else if err := ...; err != nil` 只有在前后条件真的是同一组互斥判断时才用；如果 `else` 代表一个业务状态，而里面只是执行动作后的错误处理，就把错误处理放进 `else` 块里。

常见场景：有旧资源就复用、恢复或更新；没有旧资源就创建。`id != ""`、`found`、`exists`、`attached` 这类判断是业务状态；`err != nil` 是动作失败结果，不是另一个并列业务状态。

坏味道：

```go
if existingID == "" {
	existingID, err = createResource()
	if err != nil {
		return err
	}
} else if err := reuseResource(existingID); err != nil {
	return err
}
```

更好：

```go
if existingID != "" {
	if err := reuseResource(existingID); err != nil {
		return err
	}
} else {
	existingID, err = createResource()
	if err != nil {
		return err
	}
}
```

核心判断：`existingID != ""` 和 `existingID == ""` 是业务状态；`err != nil` 只是某个状态分支里的失败结果。不要把失败结果写成另一个业务分支，读者会误以为有两套并列状态。具体到 session 场景，`southID` 是否存在就是这类业务状态。

### 共享副作用只写一次

如果 connected/orphan、success/failure、old/new 这类分支最终都会执行同一个真实副作用，就先算清分支状态，把共同副作用放到主流程里只调用一次，分支只处理差异。

坏味道：

```go
if !connected {
	if err := releaseAndMarkClosed(id); err != nil {
		logOrphanError(err)
		return err
	}
	return nil
}

if err := releaseAndMarkClosed(id); err != nil {
	logConnectedError(err)
	return err
}
detach(id)
```

更好：

```go
connected := hasSession(id)
if err := releaseAndMarkClosed(id); err != nil {
	logReapError(connected, err)
	return err
}
if !connected {
	return nil
}
detach(id)
```

同一个函数调用出现在 if/else 两边，通常说明结构还没理顺。除非两边参数、时机或错误语义确实不同，否则不要重复调用；重复会让读者误以为有两套业务路径，也更容易在下一次修改时漏改一边。

### 职责放在真正拥有数据的一层

按数据和副作用的所有权分配职责：

- API/handler 层只做参数校验、组装、转发和错误转换，不写业务生命周期规则。
- Lifecycle/reaper 负责记录活跃时间、flush touch、查找过期 session、控制回收节奏。
- Manager 负责内存 session 索引、连接状态、session detach/close、south 连接绑定；如果它拥有 resolver，sandbox claim release 也应归它。
- Store 负责持久化语义，例如 `closed_at`、只查询 active session、空更新 no-op。

如果某个类型只为执行另一个类型真正拥有的副作用而持有依赖，就把这个副作用移回真正拥有者。

避免多个 struct 无必要地重复持有同一个依赖、状态或变量。先判断谁真正拥有数据和副作用；其他类型只在调用链上临时传递它，或者通过拥有者暴露的行为使用它。

如果一个字段只是为了让另一个对象调用真正拥有者的方法，就删掉这个字段，把调用挪回拥有者，或者把依赖作为局部参数沿调用链传递。

### 生命周期入口要统一

同一个业务生命周期只能有一个清晰主入口。不要同时保留 `closeX`、`reapX`、`finalizeX`、`detachX` 几个互相绕的入口，让调用方猜应该走哪条路径。

先判断真实语义：

- 断开连接只是 transport 生命周期，不等于关闭业务 session。
- 关闭 session 才是业务生命周期结束，应该负责 release 外部资源、写关闭状态、再清理内存索引。
- 后台 reaper、主动 close、异常回滚如果最终语义都是“关闭 session”，就应该调用同一个 `CloseSession(id, closedAt)` 入口。

坏味道：

```go
func closeSession(id SessionID) error { ... }
func reapSession(id SessionID) error { ... }
func finalizeSession(id SessionID) error { ... }
```

更好：

```go
func CloseSession(id SessionID, closedAt time.Time) error { ... }
```

把不同阶段拆成私有 helper 可以，但 helper 只能表达步骤，不要变成第二套业务入口。例如：

- `finalizeSessions`：release claim + mark closed。
- `detachSession`：只处理内存索引。
- `closeConnection`：只处理 transport 断开，不假装关闭 session。

涉及时间、状态变更、错误语义时，不要在函数内部偷偷取当前时间或吞掉状态差异。调用方已经知道关闭发生在什么时候，就把 `closedAt` 显式传进来；这样测试、日志和持久化语义都更清楚。

### 保持命名和数据流直接

命名要让读者看到真实业务对象和状态变化，不要用泛化词掩盖职责不清。

- 避免 `data`、`info`、`manager`、`handler`、`helper` 这类无法说明所有权的名字，除非它们在项目里已经是稳定领域词。
- bool 参数如果让调用点看不出语义，优先拆成两个明确行为，或把策略放到拥有者内部。
- 不要新增只传递参数的中间 struct。除非它表达稳定领域概念，否则直接传参更清楚。
- 不要把同步流程改成事件、回调、channel 或状态机，除非真实并发模型要求这样做。

### 日志必须方便排查问题

只在真实状态变化处加日志：

- connection opened / closed
- session created / bound / detached / reaped / closed
- sandbox claim release success / failure
- lifecycle start / stop、flush batch、reap batch

不要在每个小函数入口打噪音日志。日志要带稳定标识，例如 `north_session`、`south_session`、`connection`、`agent`、`cutoff`、数量。

### 测试必须贴近生产流程

不要为了满足 fake 测试而改生产代码。

如果生产流程一定会构造某个依赖，测试也应该构造它。例如，把 `NewManager(..., nil)` 改成真实 fake store，而不是在生产代码里加 `if m.store == nil`。

测试 fake 要保留真实语义。真实 store 对空更新是 no-op，fake 也应该 no-op，不要记录生产中不可能出现的事件。

测试应该证明行为，不要锁死实现偶然细节。为了让测试更好写而增加生产分支、接口、导出方法或可变状态，通常是错的。

### 文档必须跟着实现走

每次更新代码、配置、测试策略或运行语义，都要主动检查文档是否需要同步。影响公共行为、数据字段语义、错误语义、配置项、运维排查方式、测试方式的改动，必须同时更新对应的 `README`、`docs`、配置示例、测试说明或必要代码注释。

不要把“补文档”留给以后。文档不更新会让下一次维护者按旧语义排查问题，这是实打实的质量缺陷。

如果判断不需要更新文档，收尾时要简短说明原因。不要沉默跳过。

### 关闭 session 语义要闭环

如果 close/reap 会写入 `closed_at`，读取路径也必须尊重这个状态：

- `GetSession` 不应把已关闭 session 当 active session 返回。
- `ListSessions` 不应继续展示已关闭且可 load 的 session。
- 如果 PR 触碰这块逻辑，测试要覆盖关闭后不再出现在 list/load 结果里。

## 审查清单

收尾前检查：

- 是否以 PR 最后守门员的标准拦住了必须改的问题。
- 净复杂度是否下降：代码更少、路径更少、状态更少、间接层更少。
- 新增代码是否用删除更多复杂度来付账。
- 没有新增无真实生产路径的 `nil` 或空值判断。
- 没有保留内联后更清楚的一行包装函数。
- 没有把 3 到 5 行的简单逻辑拆成需要来回跳转的薄 helper。
- 没有为了“看起来更模块化”新增导致总代码量增加的函数。
- 没有为了想象中的未来扩展新增接口。
- 没有为了测试方便新增生产抽象、导出方法或防御分支。
- 主流程更扁平，至少没有更深。
- `if/else` 调整没有为了压平嵌套而增加代码量。
- API 层没有混入业务逻辑。
- Lifecycle 和 Manager 的职责符合数据所有权。
- 同一业务生命周期没有保留多套 close/reap/finalize 入口；断连接和关闭业务对象语义清楚分开。
- 没有多个 struct 无必要地重复持有同一个依赖、状态或变量。
- 命名没有掩盖所有权、状态变化或真实业务语义。
- 日志记录真实状态变化，不制造噪音。
- 测试使用真实语义的 fake，并断言行为，而不是实现偶然细节。
- 已主动检查并同步相关文档；如无需更新，收尾说明理由。
- 用户已同意的修改已经落地，而不是停留在建议。
- 已执行 `gofmt`、定向测试，以及项目要求的最终检查。

## 验证

Go 仓库优先按这个顺序验证：

```bash
gofmt -w <changed-go-files>
go test ./<changed-package>
go test ./...
go vet ./...
git diff --check
```

如果仓库本身有更严格的本地规范，优先遵守仓库规范。不要每改一小点就频繁跑全量测试；等清理成型后再跑，或者在定位失败时定向跑。
